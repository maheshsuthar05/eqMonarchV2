import MonarchPageCarded from '@monarch/core/MonarchPageCarded';
import React from 'react';

function StatementPage() {
  return (
    <MonarchPageCarded
      contentTitle={'Statements'}
      contentToolbar={<></>}
      leftSidebarContent={<></>}
      content={<></>}
      innerScroll
    />
  );
}

export default StatementPage;
