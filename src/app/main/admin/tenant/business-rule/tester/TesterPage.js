import FusePageCarded from '@fuse/core/FusePageCarded';
import { makeStyles } from '@material-ui/core/styles';
import useIsMounted from 'app/common/hooks/useIsMounted';
import React, { useEffect } from 'react';
import { useDispatch, useSelector } from 'react-redux';
import DecisionRuleHeader from '../Header';
import DecisionRuleSidebarContent from '../SidebarContent';
import DeicisionRuleSidebarHeader from '../SidebarHeader';

const RuleTesterPage = ({ match, history, location }) => {

  return (
    <>
    </>
  );
};

export default RuleTesterPage;